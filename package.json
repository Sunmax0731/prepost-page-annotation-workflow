{
  "name": "prepost-page-annotation-workflow",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "投稿前チェックリストとページ注釈をブラウザ上で扱い、URLごとに確認状態を残す。",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node tools/qcds-evaluate.mjs",
    "validate": "npm test"
  },
  "keywords": [
    "chrome-extension",
    "prepost",
    "manifest-v3"
  ],
  "license": "MIT"
}
